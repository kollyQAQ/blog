[TOC]

#### 安装

```shell
yum install -y lrzsz
```

#### 下载文件

```shell
sz filename
```

#### 上传文件

```shell
rz
```

* 执行rz命令后，在弹出框中选择要上传的文件即可。 

* 说明：打开SecureCRT软件 -> Options -> session options -> X/Y/Zmodem 下可以设置上传和下载的目录。 